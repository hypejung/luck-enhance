# 이스터에그 정답지 (비공개)

이 파일은 유저에게 공개되지 않은 이스터에그 달성 조건 목록입니다.
게임 내 어디에도 조건이 힌트로 노출되지 않으며, 달성 시 타이틀(`🍀 운빨강화하기`) 옆에
아이콘 뱃지가 붙습니다. 뱃지에 마우스를 올리면 이름/설명만 보이고 조건 자체는 여전히
비공개입니다.

조건이 성립한 순간 `unlockEgg(id)`가 호출되어 `S.eggs[id] = true`로 저장되고,
같은 조건은 한 번만 발동합니다(중복 방지 가드 있음).

## 목록

| id | 뱃지 | 이름 | 달성 조건 (코드 위치) |
|---|---|---|---|
| `midnight` | 🌙 | 자정의 방문자 | 현지 시각 00시대(00:00~00:59)에 `save()`가 호출됨 (자동저장 10초 주기이므로 그 시간대에 게임이 켜져 있기만 하면 됨) — `save()` 함수 내부 |
| `blacksmith` | ⚒️ | 불굴의 대장장이 | 누적 무기 파괴 횟수(`S.destroyedCount`)가 50회 이상 — `tryEnhance()`의 파괴 분기 |
| `collector` | 🎭 | 만물 수집가 | 가방(`S.weapons`)에 8종류 무기(단검/검/쌍검/도끼/창/활/지팡이/둔기)를 동시에 전부 보유 — `addWeapon()` |
| `allin` | 🎰 | 멈출 수 없는 손 | 도박장에서 올인(`ratio >= 1`, 즉 "올인 🔥" 버튼)으로 5연속 손실(마이너스 배율) — `gamble()`, `S.allinLossStreak` |
| `noBox` | 🙈 | 박스는 필요없다 | 랜덤박스를 단 한 번도 열지 않은 채(`S.boxCount === 0`) 20층(모든 사냥터)을 해금 — `dropRoll()`의 해금 분기 |

## 참고
- 신규 이스터에그를 추가할 때는 `EASTER_EGGS` 객체(game-source.html)에 항목을 추가하고,
  이 표에도 함께 기록할 것.
- 유저에게 힌트를 주고 싶다면 이 파일이 아니라 별도 대화로 직접 안내할 것 — 게임 코드에는
  절대 조건을 노출하지 않는다.
