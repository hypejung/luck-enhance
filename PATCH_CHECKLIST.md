# 패치 체크리스트

적용 완료된 항목은 [x]로 체크. 하나씩 진행하면서 갱신.

## 🎯 밸런스/수치 조정
- [x] 만렙 확장 — "사냥터 20→40 확장"으로 재해석. 21~40층(월드2: 황혼국) 전면 오픈 완료(`FLOOR_CAP=40`), 태초 보조무기만 아직 보류 (commit 69061fc → d52aba2에서 전면 오픈)
- [x] 보조무기 추가 — 컨셉/데이터/보너스 체계에 이어 실제 게임플레이(장착·드롭·뽑기·강화·전투 훅·전용 탭 UI)까지 전부 연결 완료 (commit e2c6772, fdd16ff, c77dce1, 2c5dfe6). 남은 건 세부 밸런스 조정과 태초 등급 콘텐츠뿐 — "🗡️ 보조무기 시스템 구현" 섹션 참고
- [x] 강화 파괴확률 감소 수치 조정 및 버그 수정 — %p 차감으로 수정 + 소스 수치 재설계(신화까지 합계 8%p, +16강까지만 0%) (2026-09-16 배포, commit bd443ec)
- [x] 무기 기본 판매값 상향 — 랜덤박스 기대값(최적 박스 기준)의 50%로 재산정, 고대는 태초처럼 판매 불가 전환. 강화 판매가 곡선은 공격력 배율 대신 실제 성공확률/비용 기반 기대비용의 50%로 재설계 (2026-09-16 배포, commit 85f0b3a)
- [x] 둔기 무기특성 개선 — "파쇄의 리듬"(매 5타 강타, 무기 공격력 250% 고정피해)으로 교체, 도감 5층 세트효과 신화/고대 역전도 같이 수정 (2026-09-16 배포, commit af818fc)
- [x] 보스 장신구/무기 세트효과에 고대/태초 드롭률 3배 (2026-09-15 배포, commit ccdb2b8)
- [x] 각성 효과 밸런스 재점검 — "필멸자의 저항"(파괴·초기화 확률 감소)이 +20강 파괴확률 절벽(13%→32%)을 슬롯 하나로 거의 무력화할 만큼 강해서(도감 파괴감소 전 소스 합계 최대 17%p보다도 큼) 20%p→8%p로 하향. "완전한 개화"(공격력 고정 보너스)는 30%p→100%p로 상향. 나머지 일반/레어/에픽/전설 풀 효과는 다른 시스템(장신구·도감·보스장신구)과 비교해 비율상 이상 없음 확인 (commit 2280d1b)

## 🎉 이벤트
- [x] "강화데이" 신설 — 메이플스토리 샤이닝스타포스데이 컨셉의 하루짜리(09시~다음날 09시) 강화 우대 이벤트. 성공확률(메인 23강 기준 +5%p, 보조 20강 기준 +7%p, 기존 `ENH_BONUS_SOURCES`와 같은 캡 가중치 방식), 파괴확률(전 구간 ×0.5, 고급강화와 곱연산 시 1/4), 단계하락 확률(20강 이하 -70%, 21강 이상 -50%), 강화 골드·재료(20강 이하 -50%, 21강 이상 -30%) 전부 조정. 날짜는 `todayKey()` 기반 하드코딩 배열(`ENHANCE_EVENT_DATES`)이라 이벤트 열 때마다 날짜만 추가해서 재배포. 타이틀 아래 반짝이는 배너로 홍보. 최초 적용일 2026-09-20 (commit 34de994)

## 🌍 월드2(21~40층) 구현
- [x] FLOORS 21~40 데이터(사냥터/몬스터/재료/보스명) 추가 (commit 69061fc)
- [x] 재료 등급 매핑 20층 순환 수정(`tierIdx = floor((f%20)/5)`) — 21층 이상에서 재료 등급이 태초로 고정되던 문제 사전 방지 (commit 69061fc)
- [x] ~~도감 "보스의 무력" 마일스톤 22~40 구간 추가~~ → 되돌림. 월드2 보스 드롭은 메인 무기가 아니라 보조무기로 확정되어 "보스의 무력"은 다시 1~20층 전용으로 복원하고, 대신 아래 "보스 보조무기 수집" 트랙을 신설 (commit 80d7bec → fdd16ff에서 되돌림)
- [x] 21~40층 "보스 보조무기"(SUBWEAPON_BOSS_NAMES) 20종 + 수집 마일스톤(SUBWEAPON_BOSS_MILESTONES) 반영 — 보스 신화무기 대신 보조무기로 확정 (commit fdd16ff)
- [x] 21~40층 "고대 보조무기"(SUBWEAPON_RELICS) 20종 + 수집 마일스톤(SUBWEAPON_RELIC_MILESTONES) 반영 — 고대 에이션트 스피릿 대신 보조무기 수집형 트린켓으로 확정 (commit fdd16ff)
- [x] 21~40층 일반 장신구(ACCESSORIES) 20종 + 보스장신구(BOSS_ACCS) 20종 반영, 일부는 보조무기 효과 증폭(subAmp)/강화확률(subEnh) 스탯 포함 (commit c77dce1)
- [x] 보스 보조무기 실제 드롭 로직 — `endBossFight()`에서 1~20층은 기존 메인 보스무기, 21~40층은 `SUBWEAPON_BOSS_NAMES`를 신화 등급 고정으로 지급(`BOSS.subweaponDrop=0.4%`, 메인보다 낮게). 이 분기가 없었으면 FLOOR_CAP 40 순간 `WEAPON_BOSS_NAMES[f]` undefined로 크래시났을 것 (commit d52aba2)
- [x] 고대 보조무기 실제 획득 경로 — 애초 계획한 "고대의 도서관" 대신 `rollAncientWeapon()`과 동일한 방식(사냥터 몬스터 처치 시 초저확률 고정 드롭)으로 확정, `rollSubweaponRelic()` 신설 (commit d52aba2)
- [x] 월드2 보스 세트효과 신설 — `subBossSetFloor()`(장착 보조무기 기준)로 기존 세트효과(골드/드롭률/공격력 보너스)를 그대로 재사용, `genesisAncientDropMult()`도 두 세트효과 중 하나만 맞아도 고대/태초 드롭률 3배 적용되도록 확장 (commit d52aba2)
- [x] FLOORS 21~40 HP 시작점 재설계 — 1~20층 단순 연장이 아니라 "토벌전 9단계(raidHp(9)≈5.5조)" 언저리로 앵커, 성장 배율(층당 약 2배)은 기존과 동일하게 유지. 월드2를 "1~20층 다음"이 아니라 "토벌전/고대의 도서관에 막힌 사람이 보조무기로 뚫는 콘텐츠"로 포지셔닝. 앵커값 자체는 첫 추정치라 실플레이 반응 보고 조정 필요 (commit d52aba2)
- [x] ~~위 앵커 재조정~~ → 3차 패스에서 발견: "일반 몬스터" 체력을 토벌전 9단계급으로 잡은 게 실수였음(보스가 아니라 일반 몬스터가 그 난이도였음). `BOSS.hpMult=100`을 감안해 그 층 "보스" 체력이 토벌전 9단계급(raidHp(8)≈5.49조)이 되도록 역산, 일반 몬스터 체력은 기존값의 1/100로 재조정(21층 550억~40층 2경7500조). 상대 성장 곡선은 그대로 유지 (commit f324cb8)
- [x] `FLOOR_CAP` 20 → 40, 월드2 전면 오픈 (commit d52aba2)
- [ ] 태초 보조무기 콘텐츠 — 1~20층 태초의 유물 로스터 완성 후 착수 (보류 중)
- [x] 월드1 전용 보너스 격리 — `isWorld1BonusActive()`(S.floor < `WORLD2_START_FLOOR`=20)로 `accEff()`의 층 결부형 소스(장신구/보스장신구는 소스 층 개별 판정, 도감 세트효과·고대의 인장·고대의 유물 마일스톤은 전체 격리)를 월드2에서 스킵. 오프라인 보상의 도감 세트 보너스, "보스의 무력" 쿨타임 감소도 동일하게 격리 (commit fdd16ff, c77dce1)
- [x] 위 격리를 스탯 카테고리별로 완화 — 실플레이해보니 전체 차단이 너무 가혹하다는 피드백 반영. 공격 계열(atk/speed/critChance/critDamage/extraAtk/bossDmg)은 1~20층 소스여도 월드2에서 계속 적용, 파밍/강화 계열(gold/luck/enh)만 격리 유지. luck 격리로 보조무기 드롭률에 1~20층 장신구발 luck이 새는 것도 자동 차단됨 (commit a773a8c)
- [x] "조합"/"장신구"/"사냥터" 탭 세계별 재편 — 조합 탭은 재료→장신구 조합+재료판매로 축소, 신규 "📿 장신구" 탭에서 "🌊 푸른세계"/"🌆 황혼국" 서브탭으로 세계별 일반 장신구+보스 장신구 관리, "사냥터" 탭도 동일 서브탭으로 층 버튼을 세계별 표시(최초 진입 시 현재 층 기준 자동 선택). ~~장착 슬롯은 공유 풀 유지~~ → 아래 항목에서 세계별 독립으로 변경. 부수적으로 월드2 보스장신구 "세트 발동중" 판정이 항상 메인무기 기준(bossSetFloor)만 보던 버그도 발견해 보조무기 기준(subBossSetFloor)으로 분기 수정 (commit a773a8c)
- [x] 장신구 장착 슬롯을 전체 공유 5개 → 월드별 독립 5개(합쳐서 최대 10개)로 변경 — 월드1/월드2 장신구가 같은 슬롯을 두고 경쟁하지 않도록 `equipAcc()`/UI 카운터/`migrate()` 안전장치 전부 세계별 판정으로 수정 (commit 22885c2)
- [ ] 토벌전 40단계 확장 여부 결정 (보류 중)
- [ ] 고대의 도서관 시작점(현재 "토벌 12단계=1서고") 재조정 여부 결정 (보류 중)

## 🗡️ 보조무기 시스템 구현
- [x] `SUBWEAPON_TYPES` 8종(무기 타입별 시너지 컨셉) + 등급별 작명 테이블(레어~신화) + `SUBWEAPON_ACTIVE`("결전의 기백") + `SUB_ENH` 강화 곡선 + 강화 순환 규칙(`subEnhRiseType`) 데이터 반영 (commit e2c6772)
- [x] "보스 보조무기"/"고대 보조무기" 수집 트랙 데이터 + 마일스톤 + `S.subBossCodexSeen`/`S.subRelics` 세이브 필드(+migrate 기본값) 반영 (commit fdd16ff)
- [x] 21~40층 장신구에 보조무기 효과 증폭(subAmp)/강화확률(subEnh) 스탯 옵션 추가 (commit c77dce1)
- [x] 장착 슬롯(`S.subweapon`/`S.subEquipped`) + 보유/장착/판매 + 전용 탭("🗡️ 보조무기") UI 신설 (commit 2c5dfe6)
- [x] 사냥터 드롭 로직 — `rollSubweaponDrop()`, 월드2(21층 이상) 전용, 초안 0.15% (commit 2c5dfe6)
- [x] "보조무기 뽑기" UI/구매 로직 — `buySubweaponGacha()`, 가격 초안 고대 룬박스의 4.5배 (commit 2c5dfe6)
- [x] 강화 UI + 실행 로직 — `trySubEnhance()`, `SUB_ENH` 곡선 + `subEnhRiseType()` 기반 스탯/패시브/액티브%·지속시간 순환 상승 (commit 2c5dfe6)
- [x] 8종 패시브 효과 전투 반영 — `accEff()`가 공용 스탯(atk/speed/critChance/critDamage/extraAtk/bossDmg)을 직접 합산, spellProc/smashMultBonus는 `subweaponPassiveValue()`로 개별 훅 (commit 2c5dfe6)
- [x] 액티브 "결전의 기백" 실전 로직 — `bossFight.active` 중 경과시간(`elapsed`)이 지정 시점(0~120초)에 도달하면 1회 발동, `bossFight.dmgBonus`로 `bossDmgBonus()`에 반영, 전투 시작마다(`resetTimedStacks()`) 리셋 (commit 2c5dfe6)
- [x] 액티브 발동 시점(0~120초) 설정 UI (숫자 입력) (commit 2c5dfe6)
- [x] `accEff()`의 subAmp/subEnh/subDestroyReduce/subDropRate/subGachaDiscount 예약 스탯을 실제 강화/드롭/뽑기 로직이 전부 소비하도록 연결 (subActiveDmg/subActiveDuration은 `SUBWEAPON_BOSS_MILESTONES`가 나중에 실제 보스 보조무기를 통해서만 채워지는 값이라 아직 카운트 0 — 무해)
- [x] 1차 수치 재조정 — 강화 비용 30배 상향(신화 +30 누적 약 329억으로 뽑기 가격(약 250억)과 같은 자릿수), 액티브 "결전의 기백" 데미지를 등급별 0강 기준값(일반50%~신화400%)+메인 무기처럼 뒤로 갈수록 가팔라지는 가속 곡선으로 재설계(신화 +30 기준 1182%), 지속시간도 등급별 기준값을 두고 같은 가속 곡선 공유(신화 +30 기준 20초) (commit 647bca7)
- [x] 보조무기 뽑기 임시 잠금(`SUBWEAPON_GACHA_LOCKED=true`) — 강화 등 주변 수치가 더 안정될 때까지 구매 차단, UI에 "준비 중" 표시 (commit 647bca7)
- [x] 2차 수치 재조정 — 보스 보조무기 드롭률(0.4%, 메인 보스무기 0.7%보다 낮게)/고대 보조무기 드롭률(고대 무기와 같은 자릿수)/등급 배율(SUBWEAPON_GRADE_MULT를 액티브 기준값과 같은 비율 1:3:4:5:6.5:8:13:20로 통일)/패시브 강화 성장률(+30강 기준 최대 2배로 소폭 상향) 반영 (commit d52aba2). 일반 드롭률(SUBWEAPON_DROP_CHANCE=0.15%)은 이번엔 그대로 유지 — HP가 대폭 올라가서 킬당 시간이 길어진 만큼 시간당 드롭 기댓값은 자연히 낮아짐
- [x] 보조무기 "스탯" 재설계 — 기존 공격력% 가산 방식(`subweaponBaseAtk`)을 폐기하고, 메인 무기와 동일한 방식(`GRADES[].atk`×`SUBWEAPON_ATK_MULT(0.5)`×`enhAtkMult(강화, 등급)`)으로 매 강화마다(3주기 제한 없이) 상승하는 절대 공격력 `subweaponAtk()`로 교체. 데미지 계산식에 `weaponAtk(w) + subweaponAtk(장착중인 보조무기)`로 직접 합산. 검증: 신화 +30 기준 subweaponAtk ≈ 85.5조 vs 동급 메인무기 weaponAtk ≈ 194.8조 (비율 약 0.44) — "진짜 두 번째 무기"급 존재감 확보. 패시브/액티브는 기존 3주기 순환 그대로 유지 (commit 147e45e)
- [x] 보조무기 가방 자동판매 컨트롤 신설 — 무기 가방과 동일한 패턴(`S.subAutoSellGrade` + "지금 가방 일괄 정리" 버튼)을 "🗡️ 보조무기" 탭의 "보유 보조무기" 패널에 이식. 지정 등급 이하 + 강화0 보조무기는 드롭/뽑기 즉시 자동판매되어 가방이 찰 일 자체가 줄어듦. 부수적으로 가방이 가득 찼을 때 최약체를 강제 판매하던 기존 로직도 골드를 지급하지 않던 버그를 수정(무기 가방과 동일하게 골드 지급) (commit c0fc73c)
- [x] 보조무기 가방 용량 40 → 60으로 확장(무기 가방과 동일) + 용량 초과 시 정책 재검토 마무리 — 장착 중인 것 하나만 남아 팔 게 없는 극단적 경우에도, 기존처럼 새 보조무기를 그냥 버리지 않고 무기 가방과 동일하게 그 자체를 자동판매해서 골드로 보상 (commit 6da5941)
- [x] 3차 수치 재조정 — (1) `raidGoldReward()`가 `FLOOR_CAP-1`을 참조하던 버그 수정: `FLOOR_CAP`이 40으로 오르면서 의도(20층 고정)와 달리 `floorGold(39)`를 참조해 토벌전 골드 전체가 약 1,111배 부풀려져 있었음 → `WORLD2_START_FLOOR-1`로 고정. (2) FLOORS 21~40 HP 재설계: "일반 몬스터" 체력이 토벌전 9단계급이던 걸 "보스" 체력이 토벌전 9단계급(raidHp(8)≈5.49조)이 되도록 역산해 일반 몬스터는 1/100로 하향. (3) `floorGold()`에 월드2 전용 분기 추가 — 20층과 동일한 골드/HP 비율로 재설계(기존엔 13만분의 1까지 붕괴). (4) 몬스터 체력이 100분의 1로 줄어 킬당 시간도 크게 감소, 보조무기 드롭 기대시간이 자연히 개선(약 22시간→약 13분 추정)되어 `SUBWEAPON_DROP_CHANCE`는 그대로 유지. (5) 강화 누적비용 배율(×9)은 "골드 소비처 확보" 목적으로 그대로 유지 (commit f324cb8)
- [x] ~~보조무기 뽑기 잠금 해제 — `SUBWEAPON_GACHA_LOCKED`를 `false`로 전환~~ → 아래 항목에서 상자 시스템으로 완전히 대체(정적 플래그 자체가 사라짐)
- [x] 보조무기 UI를 독립 탭에서 가방/강화기/랜덤박스 서브탭으로 통합 + 뽑기를 등급별 상자로 재설계 — "🗡️ 보조무기" 최상위 탭 폐지, 세 탭 각각에 "⚔️ 무기 / 🗡️ 보조무기" 서브탭 토글 신설(가방=보유/장착/판매, 강화기=설명+장착중 상세/강화, 랜덤박스=상자 구매). 단일 `SUBWEAPON_GACHA` 뽑기를 무기 랜덤박스(BOXES)와 동일 구조의 5단계 `SUBWEAPON_BOXES`로 교체 — 등급 확률은 대응 무기 박스와 완전히 동일, 가격은 일반 상자를 고대 룬박스의 3배(166.93억)로 고정하고 나머지는 무기 박스 간 가격 배율(×5/×24/×120/×54,667)을 그대로 적용(834.67억/4006.4억/2조/912.57조). 해금 조건도 정적 플래그 대신 `world2Unlocked()`(21층 해금 여부)로 변경돼 월드2 해금과 동시에 자동으로 열림. 사냥터 드롭(0.15%)은 상자와 별개 경로로 그대로 유지 (commit 36bb30b)
- [ ] 고대·태초(6~7등급) 보조무기 콘텐츠 — 1~20층 태초의 유물 로스터 완성 후 착수
- [x] 보조무기 강화기 UI를 메인 무기 강화기와 완전히 동일한 구조로 재설계 — "보조무기란?" 설명은 한 줄로 축약하고, 아이템 정보 박스/능력치 그리드/실패 규칙 안내/강화 버튼+타격 이펙트(FX)/가방에서 다른 보조무기 골라 강화하기/자동 강화(목표 단계까지 반복, 골드 소진·대상 소실 시 자동 중지)까지 메인 무기 강화기와 동일한 컴포넌트·CSS 클래스(.enhFxBtn/.enhStatGrid/.enhFxZone 등)를 공유하도록 포팅. 고급강화·각성은 보조무기 전용 자원 설계가 필요해 이번 범위에서 제외(요청 시 추가) (commit 09bc6d4)
- [x] 보조무기 액티브 피해량 성장 곡선 분리·상향 — 기존엔 피해량%와 지속시간이 같은 성장 곡선(SUBWEAPON_ACTIVE_TIER_GROWTH)을 공유해서 체감이 약했음(신화 +30 기준 1181.6%). 피해량 전용 곡선(SUBWEAPON_ACTIVE_DMG_TIER_GROWTH)을 분리해 1단계 증가율은 그대로 두고 뒤로 갈수록 더 가파르게 재설계, 신화 +30 기준 2044%(목표 약 2000%)로 상향. 지속시간 곡선/수치는 변경 없음 (commit 6ee4fbe)
- [x] 보조무기 강화 UI에 "다음 강화 시 상승" 안내 문구 추가 — 이미 있었지만 실제로는 안 쓰이던 `subEnhRiseType()`을 재사용해서 다음 시도가 공격력만인지, 패시브가 같이 오르는지, 액티브 위력/지속시간 중 뭐가 같이 오르는지 미리 보여줌 (commit 59d0494)
- [x] 사냥터 층 표시를 세계별 상대 번호로 변경 — 절대 층 번호(1~40) 대신 그 세계 안에서의 상대 번호(1~20)로 표시(예: 구 25층 → 황혼국 6단계). 내부 데이터는 그대로 절대값 유지, 표시만 변환 (commit 3f18f0f)
- [x] 보조무기 강화 골드 비용 재앵커 — 메인 무기의 "초반 강화비용/1-1층 골드 = 5배" 비율을 보조무기에도 동일 적용(2-1층 골드 기준). 기존 배율(×9)은 이 비율이 3만분의 1 수준이라 사실상 공짜였음. 신화 풀강 누적비용 약 940억 → 14.99a로 대폭 상승했지만, 최상위 상자 가격 대비 비율(16.4배)은 메인 무기의 동일 비율(331배)보다 오히려 낮음 (commit 3f18f0f)
- [x] 위 재앵커를 한 단계 더 — 보조무기 강화 시점엔 골드 수급처가 더 늘어난 상태라는 점을 반영해 `SUB_ENH_COST_EXTRA_MULT`(≈20.21배)를 추가로 곱해서, 완전강화 누적비용÷상자가격 비율이 메인 무기(신화 기준 약 332.02배)와 같은 수준으로 수렴하도록 재조정. 신화 풀강 누적비용 14.99a → 302.99a (commit e4c4dd1)
- [x] "조합" 탭 재구조화 — 스크롤이 길고 지저분하다는 피드백으로 "🧪 장신구 조합"/"🌿 보유 재료" 2개 섹션으로 분리하고, 각 섹션 내부에 다시 🌊 푸른세계/🌆 황혼국 월드 서브탭을 추가(`renderEquipSubTabBar` 재사용). 재료 자동판매 목록의 층 번호도 사냥터 탭과 동일하게 세계별 상대 번호(1~20)로 표시하도록 수정. 늪지의 흑진주는 특정 세계에 속하지 않는 재료라 어느 서브탭에서든 항상 표시 (commit 83d4f01)
- [x] 큰 숫자 표기 하이브리드 변경 — 만/억/조(10^12 미만)는 기존 유지, 1,000조(10^15)부터는 경/해/자/양/구/간/정/재/극 대신 알파벳 접미사(a,b,c... 1000배마다 다음 글자)로 표기. z 이후 aa,ab...로 이어지는 공식 기반(`bigLetterLabel()`)이라 상한 없음 — 기존 `NUM_UNITS`가 극(10^48)에서 하드로 끊기던 문제도 함께 해결. `fmt()` 하나만 수정해서 전체 화면에 자동 반영 (commit f9fba81)

## 🐛 버그 수정
- [x] 월드2 일부 장신구/보스장신구 효과 설명 누락 — `accEffText()`가 subAmp/subEnh/subDestroyReduce/subDropRate/subGachaDiscount/subActiveDmg/subActiveDuration 키를 몰라서 이 스탯만 가진 항목(유민의 마지막 목걸이, 헤르마이오의 판결기록 등)의 "효과:" 란이 비어 보이던 문제. 라벨 7개 추가해서 ACCESSORIES/BOSS_ACCS 80개 전체 빈 설명 0건 확인 (commit dea49fd)
- [x] 월드2(21층) 해금 조건 버그 — 원래 의도는 "토벌전 9단계 클리어"인데, `FLOOR_CAP`을 40으로 올릴 때 다른 층과 같은 "처치 수 채우면 자동 해금" 로직을 그대로 물려받아서 20층 사냥만으로 21층이 열려버리고 있었음. `tryUnlockWorld2()`로 20층 처치 조건 + 토벌전 9단계 클리어를 모두 요구하도록 수정, 이미 버그로 열려버린 기존 세이브는 `migrate()`에서 토벌전 9단계 미클리어 시 20층으로 강제 재잠금 (commit cbdcd3e)
- [x] 재료 자동판매 버그 — 보스 처치 고급재료 보상이 S.mats를 직접 건드려 자동판매 필터를 무시하던 문제 (2026-09-16 배포, commit 4b131ae)
- [ ] 아이폰 사파리/네이버 브라우저에서 오프라인 보상이 제대로 안 걸리는 문제 — `S.lastSeen`이 `visibilitychange`/`beforeunload`/`pagehide` 이벤트에만 의존해서 갱신되는데, 이 이벤트들이 모바일(특히 화면 잠금·인앱 웹뷰 백그라운드)에서 안 잡히면 오프라인 보상 계산 자체가 스킵됨. 수정 방향: 10초 주기 자동저장 때도 `S.lastSeen` 같이 갱신 + `pageshow` 리스너 추가 (원인 진단만 완료, 아직 미수정)
