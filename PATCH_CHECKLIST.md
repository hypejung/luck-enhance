# 패치 체크리스트

적용 완료된 항목은 [x]로 체크. 하나씩 진행하면서 갱신.

## 💡 신규 컨텐츠 제안
- [ ] 월드 레이드 추가
- [ ] 강화 효과 추가
- [ ] 스탯강화 업데이트
- [ ] 상태이상 시스템 추가 — 중독/출혈/화상 등 도트형 디버프를 몬스터에게 걸어 지속 피해를 주는 시스템(기존 무기 특수효과/고대 스피릿의 개별 도트 효과들과 겹치지 않게 설계 필요)
- [x] 도박장 리뉴얼 — 기존 "배율 베팅"(4단계 테이블) 기조는 유지하고 서브탭으로 3개 미니게임 추가. 🪜 하이로우 사다리(라운드마다 성공확률↓·배당↑, 언제든 회수 가능한 push-your-luck), 🎰 등급 심볼 슬롯머신(무기 등급 아이콘 3릴, 3개 일치 시 등급별 배당, 2개 일치는 90% 환급, 전체 기대수익률 약 91%), 🎲 주사위 베팅(식보식 — 홀/짝·특정 합 10·11·트리플 중 선택, 트리플 시 홀/짝은 무조건 패배). 셋 다 손해를 보는 확률 구조(음의 기대값)는 기존과 동일 (commit 3235041)
- [x] 강화 이펙트 추가 — 강화 버튼을 누르면 즉시 결과가 나오는 대신, 무기/보조무기 강화기 패널 전체가 약 1.3~1.8초간 반짝이며 "뜸들이는" 연출 후 결과가 반영되도록 변경. 성공(골드빛 반짝임+파티클)/실패-유지·하락(먼지)/파괴·초기화(패널이 흔들리며 금이 가고 파편이 튀는 연출, 무기 파괴 모달은 0.7초 지연 표시) 3종으로 결과별 연출 분리. `tryEnhance`/`trySubEnhance`를 "검증+비용차감+연출 시작"과 "판정+리빌"(`resolveEnhance`/`resolveSubEnhance`) 두 단계로 분리, 자동강화는 이 대기시간만큼 자연스럽게 느려짐(기존 가드로 별도 처리 불필요) (commit 576a21f)

## 🌍 월드2(21~40층) 구현
- [ ] 태초 보조무기 콘텐츠 — 1~20층 태초의 유물 로스터 완성 후 착수 (보류 중)
- [ ] 토벌전 40단계 확장 여부 결정 (보류 중)
- [ ] 고대의 도서관 시작점(현재 "토벌 12단계=1서고") 재조정 여부 결정 (보류 중)

## 🗡️ 보조무기 시스템 구현
- [ ] 고대·태초(6~7등급) 보조무기 콘텐츠 — 1~20층 태초의 유물 로스터 완성 후 착수

## 📱 앱화(PWA)
- [ ] PWA(설치형 웹앱)로 전환 — `manifest.json` + 아이콘 + 서비스워커 추가해서 폰 홈화면에 설치 가능하게. 코드 변경 적고 앱스토어 심사 불필요. (참고: Capacitor 등으로 네이티브 APK/IPA까지 가는 방법도 있으나 빌드 파이프라인 부담 커서 후순위)

## 🐛 버그 수정
- [ ] 전투 시 효과 텍스트(floatText) 겹침 문제 해결
- [x] 펀치기계가 극단적으로 강한 세팅(태초 무기 등)에서 더미 체력(1e18)을 다 깎아버리면 일반 보스 처치로 오판해 120초를 못 채우고 조기 종료되던 문제 — handleKill()에서 isPunch를 먼저 걸러 체력을 다시 채우고 초과분을 누적하도록 수정 (commit c242b82)
- [ ] 아이폰 사파리/네이버 브라우저에서 오프라인 보상이 제대로 안 걸리는 문제 — `S.lastSeen`이 `visibilitychange`/`beforeunload`/`pagehide` 이벤트에만 의존해서 갱신되는데, 이 이벤트들이 모바일(특히 화면 잠금·인앱 웹뷰 백그라운드)에서 안 잡히면 오프라인 보상 계산 자체가 스킵됨. 수정 방향: 10초 주기 자동저장 때도 `S.lastSeen` 같이 갱신 + `pageshow` 리스너 추가 (원인 진단만 완료, 아직 미수정)
